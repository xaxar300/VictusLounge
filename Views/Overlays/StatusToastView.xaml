<UserControl x:Class="VictusLounge.Views.StatusToastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             IsHitTestVisible="False"
             Background="Transparent">
<Border
                Width="360"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,24,24"
                CornerRadius="14"
                BorderBrush="{DynamicResource LineBrush}"
                BorderThickness="1"
                Background="{DynamicResource PanelGradientBrush}"
                Padding="16">
            <StackPanel>
                <TextBlock x:Name="StatusTitleText"
                           Text="Готово"
                           Foreground="{DynamicResource GoldLightBrush}"
                           FontWeight="Black"
                           FontSize="15"
                           TextWrapping="Wrap" />
                <TextBlock x:Name="StatusBodyText"
                           Text="Действие выполнено."
                           Foreground="{DynamicResource MutedBrush}"
                           Margin="0,6,0,0"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>
</UserControl>
